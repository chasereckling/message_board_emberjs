<h1>Message Board</h1>

<ul>
  {{#each question in model}}
    <li>{{#link-to 'question' queston}}{{question.title}}{{question.author}}{{/link-to}}</li>
  {{/each}}
</ul>

{{#link-to 'new-question' classNames="btn btn-primary"}}Add question{{/link-to}}
